# Core HTTP & HTML parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0          # Fast HTML/XML parser for BeautifulSoup

# Multi-source job scraping (LinkedIn, Indeed, ZipRecruiter, Glassdoor)
python-jobspy>=1.1.4

# ATS Resume generation
python-docx>=1.1.0

# Gmail API (for draft creation with attachment support)
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Logging
loguru>=0.7.2

# Scheduling (daemon mode)
schedule>=1.2.1

# Type checking (optional, dev)
mypy>=1.8.0
types-requests>=2.31.0
types-PyYAML>=6.0.12

# Testing
pytest>=8.1.0
pytest-mock>=3.12.0
responses>=0.25.0    # Mocking HTTP responses in tests
